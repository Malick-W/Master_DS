#!/usr/bin/env python3# -*- coding: utf-8 -*-import syscle_prec,nb_total = None,0for ligne in sys.stdin:    cle, valeur = (ligne, 1)    nb_total += int(valeur)    print(f"nombre de lignes\t{nb_total}")# ~ for ligne in sys.stdin:    # ~ cle, valeur = ligne.split('\t', 1)    # ~ if cle == cle_prec:        # ~ nb_total += int(valeur)    # ~ else:        # ~ if cle_prec != None:            # ~ print(f"{cle_prec}\t{nb_total}")        # ~ cle_prec = cle        # ~ nb_total = int(valeur)# ~ if cle_prec != None:    # ~ print(f"{cle_prec}\t{nb_total}")